export interface User {
  id: string
  email: string
  name: string
}

export interface Session {
  id: string
  title: string
  location: string
  startDate: string
  endDate: string
  notes: string
  createdAt: string
}
